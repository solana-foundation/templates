# Phantom Connect SDK Configuration
# Get your App ID from https://portal.phantom.com

# Required: Your Phantom App ID for embedded wallet support
NEXT_PUBLIC_PHANTOM_APP_ID=your_phantom_app_id_here

# Optional: Custom Solana RPC URL (defaults to public RPC if not set)
# Recommended: Use a dedicated RPC provider like Helius, QuickNode, or Triton
NEXT_PUBLIC_SOLANA_RPC_URL=

# Optional: Solana network (mainnet-beta, devnet, testnet)
# Defaults to mainnet-beta if not set
NEXT_PUBLIC_SOLANA_NETWORK=mainnet-beta
