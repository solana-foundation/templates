# Solana Wallet Keypair (JSON array format)
# This is used to sign payment transactions for the paid endpoint
#
# Generate a new keypair: solana-keygen new --no-bip39-passphrase --outfile keypair.json

VITE_SIGNER_KEYPAIR=[YOUR_KEYPAIR_ARRAY_HERE]

# API Configuration
VITE_API_BASE_URL=http://localhost:3000
VITE_DEVNET_RPC_URL=https://api.devnet.solana.com
